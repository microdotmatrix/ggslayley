{{!--
Re-usable card for linking to posts
--}}

<article class="xc-card {{post_class}}">
  <figure class="xc-card__figure">
    {{#if feature_image}}
      <a class="xc-card__link" href="{{url}}">
        <img class="xc-card__image"
            srcset="{{img_url feature_image size="s"}} 300w,
                    {{img_url feature_image size="m"}} 600w,
                    {{img_url feature_image size="l"}} 1000w,
                    {{img_url feature_image size="xl"}} 2000w"
            sizes="(max-width: 1000px) 400px, 700px"
            src="{{img_url feature_image size="m"}}"
            alt="{{#if feature_image_alt}}{{feature_image_alt}}{{else}}{{title}}{{/if}}"
        />
      </a>
    {{else}}
      <img class="xc-card__image" src='http://placeskull.com/800/600/222222/1' alt="Placeholder Skull"/>
    {{/if}}
    <figcaption>
      <span class="xc-card__meta"><time datetime="{{date format="YYYY-MM-DD"}}">{{date format="D MMM YYYY"}}</time></span>
      <a class="xc-card__title" href="{{url}}">{{title}}</a>
    </figcaption>
  </figure>
  <div class="xc-card__content">
    <div class="xc-card__excerpt"><p>{{excerpt}}...</p></div>
    <a href="{{url}}"><strong>Read more</strong></a>
  </div>
</article>
